# Changelog

Все значимые изменения фиксируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).
Проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

---

## [1.4.0] — 2026-03-05

### Added

- Меню смены статуса вместо Enter-паузы после обработки документа (действия 1/2/3):
  - `1` — сменить статус документа на «Собран»
  - `0` — выход
  - после смены на «Собран» предлагает вернуть статус «На сборке» (откат)
- Автофильтрация документов с маркером Клеверенс в комментарии — такие документы исключаются из списка (уже обработаны на ТСД):
  - `document has been picked on the cleverence handheld terminal`
  - `документ отобран на тсд клеверенс`
- Метод `PUT` в `MoySkladClient` для обновления документов
- Функция `change_demand_state` — смена статуса через API МойСклад
- Динамическая рамка QR-кода — ширина автоматически подстраивается под длину номера отгрузки
- QR выводится для всех действий 1/2/3 (ранее только 2/3)

---

## [1.3.0] — 2026-03-05

### Added

- Вывод QR-кода в терминал после успешного сохранения xlsx (действия 2 и 3) — для сканирования на ТСД и открытия документа сборки
- QR кодирует номер отгрузки (имя файла) в формате ASCII, выводится сразу под строкой «✓ Сохранено»
- Зависимость `qrcode>=7.4` добавлена в `requirements.txt`

---

## [1.2.0] — 2026-03-05

### Added

- Бэкап позиций отгрузки перед любым изменением (действия 1/2/3) '— защита от пустого документа при сбое между DELETE и POST
- Файл бэкапа сохраняется в `output/backups/{demand_uuid}/{timestamp}_{name}.json`; первая строка — название документа (читаемо, без знания UUID)
- Действие `9` в меню — восстановление из заранее сохранённого бэкапа (выбор бэкапа из списка с датами); пункт отображается только если бэкапы есть

### Fixed

- `save_backup`: guard-проверка наличия `assortment.meta` перед записью — позиция без meta пропускается с предупреждением (не `KeyError`)

---

## [1.1.1] — 2026-03-03

### Changed

- xlsx-экспорт: удалена колонка Code128, добавлена пустая колонка «№ Коробки» (E)
- Колонка «Кол-во» (C) — выравнивание по левому краю
- Колонка «№ Коробки» (E) — выравнивание по центру (горизонталь и вертикаль), шрифт 20pt
- Высота строк данных зафиксирована (15pt) — не растягивается под крупный шрифт

---

## [1.1.0] — 2026-03-03

### Added

- Экспорт позиций отгрузки в xlsx (`output/{имя_отгрузки}.xlsx`):
  - Колонки: Артикул, Code128, EAN13, Кол-во, Ячейка
  - 1 строка = 1 позиция, значения не суммируются
  - Синяя шапка с белым жирным текстом
  - Ширина столбцов подстраивается под содержимое (не обрезает значения)
- Интерактивное меню действий вместо простого запроса `y/N`:
  - `1` — применить сортировку в МойСклад
  - `2` — применить сортировку + сохранить xlsx
  - `3` — только сохранить xlsx (без изменений в МойСклад)
  - `0` / Enter — отмена
- Флаг `--apply` теперь автоматически выбирает действие `2` (сортировка + документ)

### Changed

- xlsx сохраняется только по явному выбору пользователя (действие 2 или 3), не автоматически

### Fixed

- Ширина столбцов xlsx рассчитывается динамически по длине содержимого — длинные штрихкоды и названия не обрезаются

---

## [1.0.2] — 2026-03-03

### Fixed

- Терминал не закрывается сразу после вывода таблицы — добавлена пауза «Нажмите Enter»
- `EOFError` в `input()` больше не приводит к падению скрипта (неинтерактивный режим)
- При `--apply` паузы нет (разрешена автоматизация)

---

## [1.0.1] — 2026-03-03

### Changed

- Таблицы отгрузок и позиций теперь отображают динамическую ширину столбцов по данным
- Разделитель `─` растягивается ровно по ширине таблицы (was: фикс. 78)

### Removed

- Мёртвый код: `_hr()`, `_BORDER`, `MoySkladClient.delete()`, `MoySkladClient.put()`, `_raise_for_status()`

---

## [1.0.0] — 2026-03-03

### Added

- Получение списка отгрузок за произвольный период с фильтром по статусу (`--days`, `--state-name`)
- Интерактивный выбор отгрузки по номеру из таблицы
- Сортировка позиций по ячейке склада (`slot.name`) с натуральным порядком (A-1-2 < A-1-10 < B-1-1)
- Fallback на пользовательский атрибут товара если `slot` отсутствует (`--cell-attr`)
- Применение отсортированного порядка через batch-DELETE + POST bulk (2 запроса вместо N+1)
- Верификация после сохранения: сравнение числа строк, суммарного кол-ва, суммы и порядка позиций
- Token bucket rate limiter (40 req/3s) + retry при 429/5xx/Timeout (экспоненциальный backoff, до 5 попыток)
- Флаг `--apply` для автоподтверждения без интерактивного ввода
- Флаг `--debug` для диагностического вывода сырых ответов API
- Цветной вывод таблиц (colorama)
- Авторизация через системное хранилище ключей (keyring), токен не хранится в файлах

### Technical

- `POST /entity/demand/{id}/positions/delete` — batch-удаление позиций одним запросом (endpoint подтверждён)
- `demand["id"]` используется напрямую (не парсинг `meta.href`) для устойчивости к query-параметрам
